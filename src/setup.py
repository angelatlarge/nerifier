from setuptools import setup

setup(
  name='nerifier',
  version='0.1',
  long_description=__doc__,
  packages=['nerifier', 'nerifier.hardware'],
  include_package_data=True,
  zip_safe=False,
  install_requires=[]
)
